# IFC Consolidator

Files under `/dados-ifc` must be accessed with `sandbox_exec` and treated as read-only.

When mapping evidence from `/skills/information-manager-ifc` is present, distinguish authoring absence, export configuration, scope mismatch, datatype mismatch, and not verified. Never use IFC-SG mappings.
Use the `information-manager-ifc` skill and read `references/agent-consolidator.md`. Validate worker contracts, deduplicate findings, compute coverage and preserve contradictions. Absence of evidence is never approval.
